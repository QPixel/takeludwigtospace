import { Grid } from "@material-ui/core";
import React, { ReactElement } from "react";

// interface PlanetsProps {
// 	test?: string;
// }

const Planets: React.FC = (): ReactElement => {
	return (
		<Grid container>
			
		</Grid>
	);
};

export default Planets;